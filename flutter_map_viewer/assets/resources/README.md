Put app-bundled map images here later if you want more than the demo asset.

Recommended pattern:

- `assets/resources/floor_1.png`
- `assets/maps/floor_1.json`

Then register the image and JSON paths in `pubspec.yaml`.
