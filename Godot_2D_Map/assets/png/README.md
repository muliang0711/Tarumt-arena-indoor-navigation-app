# PNG Asset Storage

Use this folder for imported or generated PNG files that are not tile-source images.

The map itself must not be represented by one full-map PNG. Build maps from reusable tiles in `res://assets/tiles/` and scene data instead.

