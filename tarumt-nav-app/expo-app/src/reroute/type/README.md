# Reroute Types

Small type files for future reroute/wrong-way detection inputs and outputs.

These types model already-derived sources only: heading state, route graph edges, and Wi-Fi node confidence summaries.
