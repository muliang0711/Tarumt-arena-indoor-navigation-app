# PDR Type Memory

- `MotionInputSample` is transient batch input.
- `PdrPipelineResult.estimate` is the low-rate derived output for navigation input.
- Keep persistent raw sample buffers out of the app.
