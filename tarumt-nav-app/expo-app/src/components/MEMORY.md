# Components Memory

- The base map is a single PNG image.
- Component props should receive prepared coordinates, not raw TMJ objects.
- Avoid placing future sensor or PDR logic in React components.
- Navigation display components consume `NavigationUiState`.
