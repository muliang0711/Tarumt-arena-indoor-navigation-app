# Navigation Component Memory

- Instruction bar floats over the bottom of the map.
- Wrong-way warning banner floats above the instruction bar and must be obvious during walking tests.
- Stats panel stays above the map so it does not block pan/zoom.
- Display values come from `NavigationUiState`.
