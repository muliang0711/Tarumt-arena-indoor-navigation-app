# Tiled Type Memory

- Tiled JSON types mirror export shape.
- App overlay/model types should stay explicit and not leak React component props.
- Import types through `src/tiled/type` unless a local direct import is clearer.
