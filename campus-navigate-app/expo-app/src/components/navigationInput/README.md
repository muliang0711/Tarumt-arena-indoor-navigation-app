# Navigation Input Components

Debug presentation for low-rate derived navigation input.

`DerivedEstimatePanel` can replay already-derived estimates into the input boundary. These controls must not subscribe to raw sensors.
