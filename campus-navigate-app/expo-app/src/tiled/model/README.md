# Tiled Models

Pure functions for map metadata, object overlays, route paths, progress interpolation, markers, and final PNG map model assembly.

No React Native rendering code belongs here.

`routeSnapModel.ts` projects free screen estimates onto the fixed route path so the blue marker can follow live/PDR input while staying constrained to the route.
